name: Block PRs to Main

on:
  pull_request:
    branches:
      - main

jobs:
  check-branch:
    runs-on: ubuntu-latest
    steps:
      - name: 🚫 Bloquear PRs a main
        run: |
          echo "⚠️ No se permiten PRs directos a main. Usa dev como base."
          exit 1
